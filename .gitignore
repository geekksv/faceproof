# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# node / hardhat
node_modules/
artifacts/
cache/

# secrets -- never commit a private key
.env

# pipeline output (evidence, receipts, reports, downloaded match images, screenshots)
# any --out directory, not just the default
out/
out_*/

# deployments: localhost is machine-specific noise; testnet records are
# meaningful evidence and are committed deliberately.
deployments/localhost.json

# os
Thumbs.db
.DS_Store
